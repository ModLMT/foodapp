<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ThuonghieuActivity"></activity>
        <activity android:name=".activity.ThongtinuserActivity" />
        <activity android:name=".activity.GiohangActivity" />
        <activity android:name=".activity.ChitietActivity" />
        <activity android:name=".activity.DouongActivity" />
        <activity android:name=".activity.DonhangActivity" />
        <activity android:name=".activity.AnvatActivity" />
        <activity
            android:name=".activity.RegisterActivity"
            android:label="Đăng kí" />
        <activity
            android:name=".activity.LoginActivity"
            android:label="Đăng nhập" />
        <activity
            android:name=".activity.HomeActivity"
            android:label="Home" />
        <activity
            android:name=".activity.MainActivity"
            android:label="Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>